#include <bits/stdc++.h>

using namespace std;

void printArr(vector<int> &arr){
  for(int i = 0; i< arr.size(); i++){
    cout << arr[i] << " ";
  }
  cout << endl;
}
void print(int arr[], int size){
  for(int i = 0; i<size; i++){
    cout << arr[i] << " ";
  }
  cout << endl;
}


int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
  size_t test;
  cin >> test;
  for(int t = 0; i<test; i++){
    int n,m;
    cin >> n >> m;
  }

  return 0;
}